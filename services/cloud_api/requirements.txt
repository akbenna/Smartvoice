# VitaScribe Cloud API - Dependencies
# Lightweight: no ML models, uses cloud APIs for STT and LLM

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# HTTP client for provider APIs
httpx>=0.24.0

# Logging
structlog>=23.1.0

# Config
python-dotenv>=1.0.0

# Live dictation relay to Deepgram
websockets>=13.0

# Praktijkregister (alleen gebruikt als DATABASE_URL staat) en de kluis voor
# eigen sleutels van praktijken (alleen gebruikt als SLEUTELKLUIS staat)
asyncpg>=0.29
cryptography>=42
# Tijdzone voor de vervaldatum van een licentie, ook in een kale container
tzdata>=2024.1
